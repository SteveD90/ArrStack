# ============================================================================
# ArrStack Environment Configuration
# ============================================================================
# Copy this file to .env and customize the values for your environment
# DO NOT commit .env to git (it's in .gitignore)

# ============================================================================
# User and Timezone Settings
# ============================================================================
# Find your PUID and PGID by running: id <username>
# These should match the user that owns the media files on your system
# TrueNAS default apps user is typically 568:568
PUID=568
PGID=568
TZ=America/New_York

# ============================================================================
# Base Paths (TrueNAS Structure)
# ============================================================================
# Main media path - contains movies, tv, etc subdirectories
MEDIA_PATH=/mnt/tank/media

# Base path for application configurations
CONFIG_BASE=/mnt/ssd/appdata

# Base paths for data and downloads
DATA_PATH=/mnt/ssd/data
USENET_PATH=/mnt/ssd/data/usenet

# ============================================================================
# Service-Specific Configuration Paths
# ============================================================================
RADARR_CONFIG=${CONFIG_BASE}/radarr
SONARR_CONFIG=${CONFIG_BASE}/sonarr
PROWLARR_CONFIG=${CONFIG_BASE}/prowlarr
PROFILARR_CONFIG=${CONFIG_BASE}/profilarr
BAZARR_CONFIG=${CONFIG_BASE}/bazarr
SABNZBD_CONFIG=${CONFIG_BASE}/sabnzbd
SAB_STREAM_PROXY_CONFIG=${CONFIG_BASE}/sab-stream-proxy

# ============================================================================
# Service Ports
# ============================================================================
RADARR_PORT=7878
SONARR_PORT=8989
PROWLARR_PORT=9696
PROFILARR_PORT=6868
BAZARR_PORT=6767
SABNZBD_PORT=8080
SABNZBD_PORT_HTTPS=9090
SAB_STREAM_PROXY_PORT=3000

# ============================================================================
# Docker Images (using hotio images for better TrueNAS compatibility)
# ============================================================================
RADARR_IMAGE=ghcr.io/hotio/radarr:latest
SONARR_IMAGE=ghcr.io/hotio/sonarr:latest
PROWLARR_IMAGE=ghcr.io/hotio/prowlarr:latest
PROFILARR_IMAGE=santiagosayshey/profilarr:latest
BAZARR_IMAGE=ghcr.io/hotio/bazarr:latest
SABNZBD_IMAGE=ghcr.io/hotio/sabnzbd:latest

# ============================================================================
# Network Settings
# ============================================================================
NETWORK_NAME=arr-network

# ============================================================================
# SABnzbd Stream Proxy Settings
# ============================================================================
# SABnzbd API Key - get this from SABnzbd Settings > General > API Key
SAB_APIKEY=your_sabnzbd_api_key_here

# Base URL for accessing the stream proxy (external URL)
# Examples: https://usenet.yourdomain.com or http://192.168.1.100:3000
BASE_URL=http://localhost:3000

# ============================================================================
# Notes
# ============================================================================
# 1. Default PUID/PGID 568:568 is for TrueNAS apps user
# 2. Paths are structured for TrueNAS SCALE (adjust for your system)
# 3. Using hotio images - they work well with TrueNAS
# 4. Update SAB_APIKEY from your working SABnzbd instance
# 5. DATA_PATH and USENET_PATH used for atomic moves and hardlinks
# 6. Adjust paths if your TrueNAS pool/dataset names differ
